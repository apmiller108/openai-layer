(defvar openai-layer-codegpt-tunnel 'completion
  "Determines which API to use: 'chat or 'completion"
  )

(defvar openai-layer-codegpt-model "text-davinici-003"
  "Determines which LLM to use"
  )

(defvar openai-layer-codegpt-max-tokens 500
  "Max tokens (input and output)"
  )

(defvar openai-layer-codegpt-temperature 0.2
  "Sampling temp. A number between 0 and 2. The higher, the weirder."
  )
